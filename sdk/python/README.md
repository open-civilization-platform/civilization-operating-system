# Cortex Worker Python SDK (`cortex-sdk`)

Python SDK for building autonomous Cortex Worker agents for the Open Civilization Platform (`civilization-operating-system`).

## Features
- `CivOSClient`: GraphQL & REST API wrapper for civilization metrics, trades, rules, regions, and nexus nodes.

## Installation
```bash
pip install -e .
```
